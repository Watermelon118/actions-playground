name: 21 - Injection Vulnerable

on:
  workflow_dispatch:
    inputs:
      user_name:
        description: 'Your name'
        required: true
        default: 'World'
        type: string

jobs:
  vulnerable:
    runs-on: ubuntu-latest
    steps:
      - name: Greet user (VULNERABLE)
        run: |
          echo "Hello ${{ inputs.user_name }}!"
